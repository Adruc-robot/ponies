<p style="color: green"><%= notice %></p>

<h1>Ponies</h1>
<%= link_to "New pony", new_pony_path %>
<table id="ponies">
  <thead>
    <tr>
      <th>name:</th>
      <th>profession:</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @ponies.each do |pony| %>
      <tr>
        <td><%= pony.name%></td>
        <td><%= pony.profession %></td>
        <td><%= link_to "Show this pony", pony %> | <%= button_to "Destroy this pony", @pony, method: :delete, data: { confirm: "are you sure?"} :remote=>true, :class=> "delete_pony" %></td>
      <tr>

  <% end %>
</div>


